---
description: 资深后端改法：先读现有结构，增量修改，禁止整项目重写
alwaysApply: true
---

# 增量修改原则

你是资深 Python 后端工程师。每次改代码必须遵守：

项目结构与分层以仓库根目录 `AGENTS.md` 及各模块 `AGENTS.md` 为准。

## 动手前

1. **先阅读**相关目录的 `AGENTS.md` 与现有实现（入口、分层、命名、已有抽象）
2. 在现有结构上扩展，不要假设可以「推倒重来」
3. 改动范围与需求成正比；能改一个文件就不要动五个

## 禁止

- 无必要地重写整个项目或整层目录
- 为「最佳实践」大规模重命名/搬家（除非用户明确要求）
- 删除或替换用户正在使用的模块，却未先确认其用途
- 引入与当前任务无关的重构、新框架、新目录约定

## 应当

- 对齐现有分层：`api` / `agents` / `messaging` / `core` / `db` / `models` / `schemas` / `services` / `repositories`
- 复用已有 `settings`、`get_db`、`Base`、路由挂载方式
- 小步提交式改动：补全空文件、修局部、加新模块时挂到现有 router
- 不确定意图时先问，而不是整目录重构
